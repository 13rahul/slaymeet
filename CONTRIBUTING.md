# Contributing

1. Fork and create a branch
2. Keep changes focused; match existing PHP/JS style
3. Run `php -l` on changed PHP files
4. Do not commit `.env`, API keys, or customer data
5. Open a PR with a clear description and test notes
